<h1>Users</h1>

<% @users.each do |user| %>
  <h3><%= user.name %></h3>
  <p><%= user.bio %></p>
  <% if user.stories.count > 0 %>
    <p>Stories:</p>
    <% user.stories.each do |s| %>
      <p><%= link_to s.title, "/stories/#{s.id}" %></p>
    <% end %>
  <% end %>
  <p>___________</p>
<% end %>
